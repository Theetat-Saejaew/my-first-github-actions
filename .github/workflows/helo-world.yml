name: My First Workflow

on: 
    pusth:
        branches: 
            - main

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Say Hello
              run: echo "Hello, World!"
